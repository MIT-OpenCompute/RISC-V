> [!info] A HART Holds State, Not Execution
> A HART technically is not the part that executes the instructions, but simply a unit that holds state such as registers and program counter. Multiple HARTs can share a core.
